import 'package:flutter/material.dart';

class BookMarkPage extends StatelessWidget {
  const BookMarkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text("Find your Bookmark"),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}